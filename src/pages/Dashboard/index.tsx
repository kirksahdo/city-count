import { BasePageLayout } from '../../shared/layouts';

export const Dashboard: React.FC = () => {
  return (
    <BasePageLayout title="Dashboard" toolBar={<>ToolBar</>}>
      Test
    </BasePageLayout>
  );
};
